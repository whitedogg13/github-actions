name: Hello

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: echo hello
        run: echo "hello github actions"
      - name: echo hello again
        run: |
          node -v
          npm -v
